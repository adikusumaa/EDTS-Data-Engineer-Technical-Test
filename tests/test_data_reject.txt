python -c "
from main import read_csv, split_duplicates, transform_data, prep_df_db, get_db_connection, insert_dataframe
df = read_csv('/app/example/scrap.csv')
_, reject = split_duplicates(df)
reject_trans = transform_data(reject)
reject_db = prep_df_db(reject_trans)

conn = get_db_connection()
insert_dataframe(conn, reject_db, 'data_reject')
conn.close()
print('Data reject inserted successfully')
"